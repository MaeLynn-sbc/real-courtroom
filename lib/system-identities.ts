// Stable identifiers for seeded system rows (prisma/seed.ts) that
// runtime app code needs to look up by — a single source of truth so
// the seed script and any service resolving these stay in sync.
export const WEBSITE_SYSTEM_USER_EMAIL = "website@thecourtroom.local";
export const PAY_AT_VENUE_PAYMENT_METHOD_KEY = "PAY_AT_VENUE";
